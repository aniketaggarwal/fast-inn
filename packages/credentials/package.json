{
  "name": "credentials",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "jose": "^5.9.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.5",
    "vitest": "^2.0.5"
  }
}
